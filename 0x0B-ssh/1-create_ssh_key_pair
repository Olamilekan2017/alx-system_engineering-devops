#!/usr/bin/env bash

# Generate RSA key pair with 4096 bits
ssh-keygen -t rsa -b 4096 -C "Generated by 1-create_ssh_key_pair" -f school

# Encrypt the private key with passphrase "betty"
openssl rsa -aes256 -in school -out school.enc -passout pass:betty

# Remove the original private key
rm school
